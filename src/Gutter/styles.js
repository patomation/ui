import config from '../config.js';

export default {
  horizontal:{
    paddingBottom: config.size.gutters
  },
  vertical: {
    paddingLeft: config.size.gutters,
    paddingBottom: 0,
    display: 'inline',
  }
}
