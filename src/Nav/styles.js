import config from '../config.js'

export default {
  container: {
    color: '#ffffff',
    display: 'flex',
    background: config.color.violet,
    padding: config.size.gutters,
    alignItems: 'center'
  }
}
